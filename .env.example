# Leave empty when the frontend is served behind the same-origin /api proxy.
VITE_API_BASE_URL=

# Local Vite proxy target (used by vite.config.ts, not exposed to browser code).
VITE_DEV_API_TARGET=https://168.107.61.81
